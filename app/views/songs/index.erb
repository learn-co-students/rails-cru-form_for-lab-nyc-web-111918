<h1> Song Index Page </h1>

<% @songs.each do |song| %>
<p>
  <%= link_to song.name, song_path(song) %>
  </p><br />
<% end  %><br />

<%= button_to "New", new_song_path, :id => "open-contacts-dialog-btn",
      :class => "inbox-sf-add-btn tip", :method => :get %>
