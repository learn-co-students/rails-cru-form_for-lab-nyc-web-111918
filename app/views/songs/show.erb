<h1> My Song Show Page </h1>

<h1> Song Name: <%= @song.name %></h1>
<label>My Genre:  <%= link_to @song.genre.name, genre_path(@song.genre.id) %> </label>
<label>My Artist: <%= link_to @song.artist.name, artist_path(@song.artist.id) %> </label>
