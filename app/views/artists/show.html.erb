<h1>Artist </h1>
<h1> <%= @artist.name %></h1> <br />
<p>
  <%= @artist.bio %>
  </p>

<%= button_to "Edit Artist", edit_artist_path, :id => "open-contacts-dialog-btn",
      :class => "inbox-sf-add-btn tip", :method => :get %>

<%= button_to "Home", artists_path, :id => "open-contacts-dialog-btn",
      :class => "inbox-sf-add-btn tip", :method => :get %>
